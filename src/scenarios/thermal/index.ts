import { makeStubScenario } from '../stub'

/** REPLACE ME: espresso-boiler FOPDT scenario (owned by the thermal teammate). */
export const thermalScenario = makeStubScenario('thermal', 'Boiler (dead time)')
