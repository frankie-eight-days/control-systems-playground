import { makeStubScenario } from '../stub'

/** REPLACE ME: DC motor position servo scenario (owned by the motor teammate). */
export const motorScenario = makeStubScenario('motor', 'Motor position')
